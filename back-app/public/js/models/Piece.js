class Piece {
    constructor(num, libelle, piece, numEB) {
        this.num = num;
        this.libelle = libelle;
        this.piece = piece;
        this.numEB = numEB;
    }
}

export default Piece;